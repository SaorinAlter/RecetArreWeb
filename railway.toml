[build]
builder = "nixpacks"

[deploy]
startCommand = "ls out/ && dotnet out/RecetArreWeb.dll --urls http://0.0.0.0:$PORT"

[build.env]
NIXPACKS_BUILD_CMD = "dotnet restore && dotnet publish -c Release -o out"